import style from 'styled-components';

export const BagRedeemContainer = style.div`
	align-self: flex-end;
	position: absolute;
	top: 0.5rem;
	right: 0.3rem;
	z-index: 300;
`;

export const Bag = style.img`
	position: relative;
	right: -0.8rem;
	width: 65%;
`;
